# TODO
3. Define codepoint as a struct so that we can put methods on it?
4. String should take a charset
5. Handle Potential BOM
6. Install README under /usr/share/doc
8. update clean script to use clean cmake target
9. add tree-sitter-dep subproject to be build if tree-sitter is not available
  - for the generate-unicode-data tool
10. Rename charsets to encodings
